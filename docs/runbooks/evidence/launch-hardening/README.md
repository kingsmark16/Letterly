# Launch-hardening drill evidence

Each approved restore drill gets one UTC-dated Markdown record in this
directory. Evidence must contain synthetic values only. Use the template and
do not commit connection strings, credentials, cookies, report messages, page
content, visitor responses, media keys, or raw identity.

The temporary Neon branch is removed only after the API owner and the security
or privacy reviewer have approved the record.
