# Neon restore drill — `2026-08-26` / `restore-drill-20260826`

## Status

- Result: `PENDING`
- Source environment: `staging`
- Restored branch: `restore-drill-20260826` (`br-restless-waterfall-azn7p5z9`)
- Commit: `5f68200`
- Prisma migration version: `20260825100000_add_question_finish_state`
- Fixture: synthetic only (`restore-drill-20260826`)

## Timing

- Restore point UTC: `2026-08-26T00:46:14Z` (Neon parent timestamp)
- Restore started UTC: `2026-08-26T05:16:12Z`
- Migrations complete UTC: `2026-08-26T05:48:38Z`
- Smoke checks complete UTC: `2026-08-26T05:48:38Z`
- RPO: `0 minutes` (no post-restore-point source write was recorded for this synthetic drill)
- RTO: `32.4 minutes` (restore start to final successful verification; target ≤ 60)

## Smoke results

| Check                       | Result | Evidence reference |
| --------------------------- | ------ | ------------------ |
| API health                  | `PASS` | HTTP `200`; `{ "status": "ok", "service": "api" }` |
| Public synthetic page       | `PASS` | HTTP `200`; `Cache-Control: no-store`; `X-Robots-Tag: noindex, nofollow, noarchive` |
| Non-admin denied            | `PASS` | HTTP `403`; code `ADMIN_REQUIRED` |
| Admin report projection     | `PASS` | HTTP `200`; projection contained no page/content/images/visitor-response fields |
| Moderation action and audit | `PASS` | Review `200`, idempotent replay `200`, appeal `200`, audit list `200` with review and appeal events |
| Stale version conflict      | `PASS` | HTTP `409`; code `STALE_MODERATION_VERSION` |
| Retention worker metric     | `PASS` | `RETENTION_SUCCEEDED` audit event (`claimed: 3`, `deleted: 3`) |

## Notes and remediation

- Neon created the temporary branch from production branch `br-flat-math-azu86vkp`; the production branch was not modified.
- Neon cold-start connection timeouts occurred during the first attempts. Retrying after the compute became ready produced the passing results above.
- The synthetic creator, session, published page, slug reservation, report, moderation records, appeal, and synthetic administrator/session were removed from the temporary branch. Cleanup counts were zero afterward.
- No production page text, media key, visitor response, credential, cookie, or raw identity was written to this evidence.

## Approvals

- API owner: `<name>`, `<UTC timestamp>`, `<approved / blocked>`
- Security or privacy reviewer: `<name>`, `<UTC timestamp>`, `<approved / blocked>`
- Temporary branch removed after approval: `not yet — both approvals required`
